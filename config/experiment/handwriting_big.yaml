# @package _global_

lightning_module:
  optimizer:
    lr: 1e-4

  network: # 60M parameter architecture
    invariance_layer:
      hidden_dims: [512]

    encoder:
      in_dim: 512
      input_dim: 512
      ffn_dim: 1024

num_nodes: 4

data_module:
  batch_size: 2

trainer:
  accumulate_grad_batches: 8
